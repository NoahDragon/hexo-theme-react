extends base

block title
    title= "test"

block vars
    - var bodyClass = 'ring-blue-skin'
    - var p = page.posts.toArray()
    - var formType = 'human'

block spinner
    .spinner-double-dot-out

block hero
    section#hero.hero.full-height.valign-wrapper.scrollspy
        #ring.ring
        h1.center Digital &amp; creative

block skin-script
    script(src='./assets/js/skins/min/ring.min.js')